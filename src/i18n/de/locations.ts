// Deutsche Texte für den Bereich "locations"; Schlüssel und Funktionen wie in ../en/locations.ts.

import type { Shape } from "..";
import type { locations as source } from "../en/locations";

export const locations: Shape<typeof source> = {};
